# Scripts

This directory is for repeatable development and validation scripts.

Expected categories:
- `dev/` local developer workflows
- `deploy/` remote deployment to `macmini`
- `validate/` protocol and printer verification
- `ci/` scripts called by GitHub Actions
