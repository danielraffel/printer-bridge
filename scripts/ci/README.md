# CI Scripts

This directory is reserved for scripts invoked by GitHub Actions or other CI systems.

The goal is to keep CI logic in scripts rather than embedding complex shell directly inside workflow YAML.
