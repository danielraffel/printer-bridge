# Tests

This directory is for test code and test assets.

Planned structure:
- `fixtures/` static sample printer data and captures
- `integration/` protocol and subsystem integration tests
- `e2e/` remote or hardware-assisted validation flows
